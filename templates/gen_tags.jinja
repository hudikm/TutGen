{%- set ns = namespace(hl_lines='', code_block="") -%}
{%- for commit in element.commits -%}
<!--tgen tag={{ commit.tag  }} {{("suffix=" + element.suffix) if element.suffix != '' and  element.suffix is not none}} {{("prefix=" + element.prefix) if element.prefix != '' and element.prefix is not none}}-->
<!--end-->
{% endfor %}

